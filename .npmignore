# IDE and tooling
/.vscode
.DS_Store
.gitignore

# Source and build config (consumers use dist/)
/src
/scripts
rollup.config.mjs
tsconfig.json

# Storybook and docs site (dev-only). Component docs: package.json "files" uses docs/*.md (not the whole /docs tree).
/.storybook
/storybook-static
/website
/templates
llms.config.js

# Dependencies and lockfile
/node_modules
package-lock.json