import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as FilterBarStories from "./filter-bar.stories";

<Meta title="atoms/FilterBar/Documentation" />

# FilterBar

`FilterBar` collects search, select, multi-select, and date-range values for filtering a table or list. Bar fields update immediately; drawer fields apply on **Apply**. The app filters row data and passes the result to `Table`.

## Usage

```jsx
import { FilterBar, Table } from "cleanplate";

<FilterBar
  fields={[
    { id: "q", type: "search", label: "Search", placement: "bar" },
    { id: "status", type: "select", label: "Status", placement: "bar", options: statusOptions },
    { id: "owner", type: "multiSelect", label: "Owner", placement: "drawer", options: ownerOptions },
    { id: "due", type: "dateRange", label: "Due", placement: "drawer" },
  ]}
  values={filters}
  onChange={setFilters}
/>
<Table columns={columns} data={filteredRows} />
```

Add `values` and `onChange` after converting from the HTML prototype in `docs/FilterBar.md`. Pass `buttonLabel` to rename the drawer trigger (default `Filters`). A committed drawer count is appended, and the button turns solid while those filters are applied.

## Responsive alignment

This playground has no controls. It puts search, selects, multi-selects, and two date ranges on the bar, with two more fields behind **More filters**. Resize the preview. Fields wrap onto the next line, and their left edge stays lined up with the table because both use a 16px horizontal inset.

<Canvas of={FilterBarStories.Responsive} />
