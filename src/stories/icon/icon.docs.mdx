import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import { ArgsTable } from "@storybook/addon-docs";
import { Icon } from "../../index";
import { ICON_CATEGORIES, MATERIAL_ICON_NAMES } from "../../components/icon/material-icon-names";

<Meta title="atoms/Icon/Documentation" />

# Icon

The `Icon` component is a versatile and reusable element for displaying scalable vector icons, supporting various sizes, colors, and custom classes. It seamlessly integrates into user interfaces to enhance visual appeal and provide intuitive, meaningful representations using **Google Material Symbols**.

### Installation

Insert the script below into the head tag of your `index.html`:

```
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
```

### Usage

```
import { Icon } from "cleanplate";

const App = () => {
  return (
    <Icon
        size="medium"
        name="cloud_upload"
    />
  );
}
```

### Props

<ArgsTable of={Icon} />

### Icon names (by category)

Icons are sourced from the generated `material-icon-names` module. Below are icons grouped by category. Use the `name` prop with any of the **{MATERIAL_ICON_NAMES.length}** available Material Icon names for full TypeScript autocomplete.

{Object.entries(ICON_CATEGORIES).filter(([, icons]) => icons.length > 0).map(([category, icons]) => (
  <div key={category}>
    <h4 style={{ textTransform: 'capitalize', marginTop: '1.5rem' }}>{category}</h4>
    <IconGallery>
      {icons.map((iconName) => (
        <IconItem key={iconName} name={iconName}>
          <Icon name={iconName} />
        </IconItem>
      ))}
    </IconGallery>
  </div>
))}
